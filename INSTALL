DEPENDENCIES:
	C programming enviroment
	libusb
	libbluetooth
	libgd
        libsndfile

INSTALL:
	Run 'make' to compile the library.
	Run 'make install' as root to copy the libnxt.a to /usr/lib and nxt.h to /usr/include
