DEPENDENCIES:
	(GNU) C programming enviroment
	libusb
	libbluetooth
	libgd
        libsndfile
        sdl
        fuse

INSTALL:
        Edit Makefile.config to adjust settings
	Run 'make' to compile
	Run 'make install' to install aNXT to /usr/local
